import 'package:flutter/material.dart';

class AuctionView extends StatelessWidget {
  const AuctionView({Key? key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      child: Center(
        child: Text('auction screen'),
      ),
    );
  }
}